# .env.example
# Demotivator AI Environment Configuration

# Model configuration
# OPENAI_BASE_URL should point to the OpenAI-compatible vLLM server root.
# Example: http://127.0.0.1:8000 (the code will ensure /v1 is appended if needed)
MODEL_NAME=openai/gpt-oss-20b
OPENAI_BASE_URL=http://127.0.0.1:8000

# Request handling
REQUEST_TIMEOUT=30
MAX_CONCURRENCY=5
MEMORY_TURNS=4

# Generation parameters
DEFAULT_TEMP=0.7
FIRST_TOKEN_SLA=2.0
MAX_RESPONSE_CHARS=100000

# Server configuration
PORT=8000

# SYSTEM_PROMPT:
# The full multi-line system prompt is included by default in backend/app.py (recommended).
# If you want to *override* it at runtime, provide a short single-line string here
# (avoid embedding literal newlines in `.env` files).
#
# Example short override:
# SYSTEM_PROMPT="You are Demotivator AI. Keep replies short, dark, and end with VERDICT line."
SYSTEM_PROMPT=""
